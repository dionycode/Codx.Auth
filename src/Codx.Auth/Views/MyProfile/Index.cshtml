@model MyProfileViewModel

@{
    ViewData["Title"] = "My Profile";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">Home</a></li>
        <li class="breadcrumb-item active" aria-current="page">My Profile</li>
    </ol>
</nav>

<div class="row">
    <div class="col-lg-12">

        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                @TempData["SuccessMessage"]
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }

        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                @TempData["ErrorMessage"]
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }

        <div class="card">
            <div class="card-header">
                <nav>
                    <div class="nav nav-tabs" id="nav-tab" role="tablist">
                        <a class="nav-item nav-link active" id="nav-details-tab" data-toggle="tab" href="#nav-details" role="tab" aria-controls="nav-details" aria-selected="true">Details</a>
                        <a class="nav-item nav-link" id="nav-claims-tab" data-toggle="tab" href="#nav-claims" role="tab" aria-controls="nav-claims" aria-selected="false">Claims</a>
                        <a class="nav-item nav-link" id="nav-roles-tab" data-toggle="tab" href="#nav-roles" role="tab" aria-controls="nav-roles" aria-selected="false">Roles</a>
                        <a class="nav-item nav-link" id="nav-tenant-memberships-tab" data-toggle="tab" href="#nav-tenant-memberships" role="tab" aria-controls="nav-tenant-memberships" aria-selected="false">Tenant Memberships</a>
                        <a class="nav-item nav-link" id="nav-company-memberships-tab" data-toggle="tab" href="#nav-company-memberships" role="tab" aria-controls="nav-company-memberships" aria-selected="false">Company Memberships</a>
                        <a class="nav-item nav-link" id="nav-manage-tab" data-toggle="tab" href="#nav-manage" role="tab" aria-controls="nav-manage" aria-selected="false">Manage</a>
                    </div>
                </nav>
            </div>
            <div class="card-body">


                <div class="tab-content" id="nav-tabContent">
                    <div class="tab-pane fade show active" id="nav-details" role="tabpanel" aria-labelledby="nav-details-tab">
                        <div class="row">
                            <div class="col-lg-12">

                                <input hidden asp-for="@Model.Id" />

                                <div class="form-group">
                                    <label>UserName</label>
                                    <input asp-for="@Model.UserName" class="form-control" readonly />
                                </div>

                                <div class="form-group">
                                    <label>Email</label>
                                    <input asp-for="@Model.Email" class="form-control" readonly />
                                </div>

                                  <div class="form-group">
                                    <label>First Name</label>
                                    <input asp-for="@Model.FirstName" class="form-control" readonly />
                                </div>

                                <div class="form-group">
                                    <label>Middle Name</label>
                                    <input asp-for="@Model.MiddleName" class="form-control" readonly />
                                </div>

                                <div class="form-group">
                                    <label>Last Name</label>
                                    <input asp-for="@Model.LastName" class="form-control" readonly />
                                </div>

                                <div class="form-group">
                                    <label>Default Company</label>
                                    <input asp-for="@Model.CompanyName" class="form-control" readonly />
                                </div>

                                <div class="form-group">
                                    <label>Default Tenant</label>
                                    <input asp-for="@Model.TenantName" class="form-control" readonly />
                                </div>

                            </div>
                        </div>
                    </div>
                    <div class="tab-pane fade" id="nav-claims" role="tabpanel" aria-labelledby="nav-claims-tab">                       
                        <br /> 
                        <span>These are my claims.</span>
                        <br /> <br />
                        <table class="table table-bordered"
                               id="userClaimTable"
                               data-toggle="table"
                               data-query-params="userClaimTableParams"
                               data-pagination="true"
                               data-side-pagination="server"
                               data-page-size="5"
                               data-page-list="[5,10,25,50,100,200,All]"
                               data-ajax="userClaimAjaxRequest">

                            <thead>
                                <tr>
                                    <th data-field="claimType">Type</th>
                                    <th data-field="claimValue">Value</th>                               
                                </tr>
                            </thead>

                        </table>

                    </div>

                    <div class="tab-pane fade" id="nav-roles" role="tabpanel" aria-labelledby="nav-roles-tab">                      
                        <br />                         
                        <span>These are my roles.</span>
                        <br /> <br />
                        <table class="table table-bordered"
                               id="userRoleTable"
                               data-toggle="table"
                               data-query-params="userRoleTableParams"
                               data-pagination="true"
                               data-side-pagination="server"
                               data-page-size="5"
                               data-page-list="[5,10,25,50,100,200,All]"
                               data-ajax="userRoleAjaxRequest">

                            <thead>
                                <tr>
                                    <th data-field="role">Role</th>                                
                                </tr>
                            </thead>

                        </table>
                    </div>

                    <div class="tab-pane fade" id="nav-tenant-memberships" role="tabpanel" aria-labelledby="nav-tenant-memberships-tab">
                        <br />
                        <span>These are my tenant memberships.</span>
                        <br /> <br />
                        <table class="table table-bordered"
                               id="myTenantMembershipTable"
                               data-toggle="table"
                               data-query-params="myTenantMembershipTableParams"
                               data-pagination="true"
                               data-side-pagination="server"
                               data-page-size="5"
                               data-page-list="[5,10,25,50,100,200,All]"
                               data-ajax="myTenantMembershipAjaxRequest">
                            <thead>
                                <tr>
                                    <th data-field="tenantName">Tenant</th>
                                    <th data-field="roles">Roles</th>
                                    <th data-field="status">Status</th>
                                    <!-- <th data-field="joinedAt">Joined</th> -->
                                    <th data-field="membershipId" data-width="200" data-formatter="myTenantMembershipRowActionButtons"></th>
                                </tr>
                            </thead>
                        </table>
                    </div>

                    <div class="tab-pane fade" id="nav-company-memberships" role="tabpanel" aria-labelledby="nav-company-memberships-tab">
                        <br />
                        <span>These are my company memberships.</span>
                        <br /> <br />
                        <table class="table table-bordered"
                               id="myCompanyMembershipTable"
                               data-toggle="table"
                               data-query-params="myCompanyMembershipTableParams"
                               data-pagination="true"
                               data-side-pagination="server"
                               data-page-size="5"
                               data-page-list="[5,10,25,50,100,200,All]"
                               data-ajax="myCompanyMembershipAjaxRequest">
                            <thead>
                                <tr>
                                    <th data-field="tenantName">Tenant</th>
                                    <th data-field="companyName">Company</th>
                                    <th data-field="roles">Roles</th>
                                    <th data-field="status">Status</th>
                                    <!-- <th data-field="joinedAt">Joined</th> -->
                                    <th data-field="membershipId" data-width="200" data-formatter="myCompanyMembershipRowActionButtons"></th>
                                </tr>
                            </thead>
                        </table>
                    </div>

                    <div class="tab-pane fade" id="nav-manage" role="tabpanel" aria-labelledby="nav-manage-tab">
                        <br />  <br />                        
                                             
                        <a asp-action="ChangePassword" asp-controller="MyProfile" class="btn btn-outline-primary">Change Password</a>
                        
                        @* Two-Factor Authentication Section *@
                        @if (Model.CanUseTwoFactor)
                        {
                            <br /><br />
                            <h5>Two-Factor Authentication</h5>
                            <p class="text-muted">Add an extra layer of security to your account with two-factor authentication.</p>
                            
                            @if (Model.TwoFactorEnabled)
                            {
                                <div class="alert alert-success d-inline-flex align-items-center">
                                    <i class="fas fa-shield-alt me-2"></i>
                                    <span>Two-Factor Authentication is <strong>enabled</strong></span>
                                </div>
                                <br />
                                <form asp-action="DisableTwoFactor" asp-controller="MyProfile" method="post" style="display: inline;">
                                    <button type="submit" class="btn btn-outline-danger" 
                                            onclick="return confirm('Are you sure you want to disable Two-Factor Authentication? This will make your account less secure.')">
                                        <i class="fas fa-shield-alt"></i> Disable 2FA
                                    </button>
                                </form>
                            }
                            else
                            {
                                <div class="alert alert-warning d-inline-flex align-items-center">
                                    <i class="fas fa-exclamation-triangle me-2"></i>
                                    <span>Two-Factor Authentication is <strong>disabled</strong></span>
                                </div>
                                <br />
                                <form asp-action="EnableTwoFactor" asp-controller="MyProfile" method="post" style="display: inline;">
                                    <button type="submit" class="btn btn-outline-success">
                                        <i class="fas fa-shield-alt"></i> Enable 2FA
                                    </button>
                                </form>
                                <br /><br />
                                <small class="text-muted">
                                    <i class="fas fa-info-circle"></i>
                                    When enabled, you'll receive a verification code via email each time you log in.
                                </small>
                            }
                        }
                        else
                        {
                            <br /><br />
                            <h5>Two-Factor Authentication</h5>
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle"></i>
                                Two-Factor Authentication is not available for external login accounts. 
                                Your external provider (Google, Microsoft, etc.) handles authentication security.
                            </div>
                        }
                        
                        @if (Model.IsGoogleAuthEnabled)
                        {
                            <br /><br />
                            <h5>Google Account</h5>
                            @if (Model.HasGoogleAccount)
                            {
                                <button type="button" class="btn btn-outline-success" disabled>
                                    <i class="fab fa-google"></i> Google Account Connected
                                </button>
                                <a asp-action="DisconnectGoogle" asp-controller="MyProfile" class="btn btn-outline-danger ml-2">
                                    Disconnect Google
                                </a>
                            }
                            else
                            {
                                <a asp-action="ConnectGoogle" asp-controller="MyProfile" class="btn btn-outline-primary">
                                    <i class="fab fa-google"></i> Link Google Account
                                </a>
                            }
                        }

                        @if (Model.IsMicrosoftAuthEnabled)
                        {
                            <br /><br />
                            <h5>Microsoft Account</h5>
                            @if (Model.HasMicrosoftAccount)
                            {
                                <button type="button" class="btn btn-outline-success" disabled>
                                    <i class="fab fa-microsoft"></i> Microsoft Account Connected
                                </button>
                                <a asp-action="DisconnectMicrosoft" asp-controller="MyProfile" class="btn btn-outline-danger ml-2">
                                    Disconnect Microsoft
                                </a>
                            }
                            else
                            {
                                <a asp-action="ConnectMicrosoft" asp-controller="MyProfile" class="btn btn-outline-primary">
                                    <i class="fab fa-microsoft"></i> Link Microsoft Account
                                </a>
                            }
                        }

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts
{
    <script>
        var modelid = '@Model.Id';
    </script>

    <script>
        function userClaimAjaxRequest(params) {
            var url = '@Url.Action("GetMyClaimsTableData", "MyProfile")';
            $.get(url + '?' + $.param(params.data)).then(function (res) {
                params.success(res)
            })
        };
        function userClaimTableParams(params) {
            return params
        }
        function userClaimRowActionButtons(index, row) {
            return '<a class="btn btn-outline-info btn-sm" href="/UserClaims/Details/' + row.id + '" title="Show record details"><i class="fa fa-file-alt"></i></a> ' +
                '<a class="btn btn-outline-warning btn-sm" href="/UserClaims/Edit/' + row.id + '" title="Edit this record"><i class="fa fa-edit"></i></a> ' +
                '<a class="btn btn-outline-danger btn-sm" href="/UserClaims/Delete/' + row.id +'" title="Delete this record"><i class="fa fa-trash-alt"></i></a>'
        }

    </script>

    <script>
        function userRoleAjaxRequest(params) {
            var url = '@Url.Action("GetMyRolesTableData", "MyProfile")';
            $.get(url + '?' + $.param(params.data)).then(function (res) {
                params.success(res)
            })
        };
        function userRoleTableParams(params) {
            return params
        }
        function userRoleRowActionButtons(index, row) {
            return  '<a class="btn btn-outline-danger btn-sm" href="/UserRoles/Delete/?userid=' + modelid + '&roleid=' + row.roleId +'" title="Delete this record"><i class="fa fa-trash-alt"></i></a>'
        }

    </script>

    <script>
        function myTenantMembershipAjaxRequest(params) {
            var url = '@Url.Action("GetMyTenantMembershipsTableData", "MyProfile")';
            $.get(url + '?' + $.param(params.data)).then(function (res) {
                params.success(res)
            })
        };
        function myTenantMembershipTableParams(params) {
            return params
        }
        function myTenantMembershipRowActionButtons(value, row) {
            return '<a class="btn btn-outline-secondary btn-sm" href="/MyProfile/ManageTenant/' + row.tenantId + '" title="Go to Tenant Details"><i class="fa fa-building"></i></a> ' +
                '<a class="btn btn-outline-info btn-sm" href="/Memberships/' + value + '" title="Go to Membership Details"><i class="fa fa-id-card"></i></a>'
        }

        function myCompanyMembershipAjaxRequest(params) {
            var url = '@Url.Action("GetMyCompanyMembershipsTableData", "MyProfile")';
            $.get(url + '?' + $.param(params.data)).then(function (res) {
                params.success(res)
            })
        };
        function myCompanyMembershipTableParams(params) {
            return params
        }
        function myCompanyMembershipRowActionButtons(value, row) {
            return '<a class="btn btn-outline-secondary btn-sm" href="/MyProfile/ManageTenantCompanyDetails/' + row.companyId + '" title="Go to Company Details"><i class="fa fa-sitemap"></i></a> ' +
                '<a class="btn btn-outline-info btn-sm" href="/Memberships/' + value + '" title="Go to Membership Details"><i class="fa fa-id-card"></i></a>'
        }
    </script>
}